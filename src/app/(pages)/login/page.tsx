import FormLogin from "@/app/(pages)/login/components/formLogin"

export default async function Login() {
    return (
        <section>
            <h2>Login</h2>
            <FormLogin />
        </section>
    )
}
